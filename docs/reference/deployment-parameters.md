# Unselected parameters and admission dependencies

Kind: reference

### Fixed direction versus unselected parameters

This design defines mechanisms and boundaries in detail. Some choices still require a concrete implementation experiment, a deployment profile, or owner preference. They are collected here so an implementation agent does not invent them while working on an unrelated feature.

A parameter is not permission to postpone an acceptance requirement indefinitely. It must be set before work depending on it is released or before a profile claiming it is admitted.

Owner supplies consequential preference/risk authority; Architect owns design sufficiency and
qualified contracts; Planner and Estimator own dependent delivery scope/estimates; Reviewer
independently evaluates evidence; Factory enforces the effective release/admission rules.
These are responsibility roles, not claims that qualification has already been completed.

| Parameter group | Required evidence and latest safe boundary |
|---|---|
| RP-01–05, RP-11, RP-16 | Architect must establish credible feasibility for the chosen boundary before design release; exact host/runtime/storage/bootstrap/code-tool/provider tuples and adversarial conformance evidence are required before their admission or a dependent guarantee is claimed. A disqualifying result reopens design. |
| RP-06–10, RP-12–13 | Planning fixes project thresholds, workload, budgets and scheduling/retention controls before dependent Work Item execution release; estimates use those inputs. Acceptance never selects them after results are known. |
| RP-14–15 | Source/criterion mapping, applicability and overlay targets must suffice before a blocking evaluation uses them; unresolved official-source ambiguity blocks that gate. |
| RP-17–18, RP-21–23 | Architect defines semantic boundaries before decomposition; exact interface/schema/prompt/projection/rendering contracts, authority and positive/adverse fixtures precede producer/consumer admission. Owner approves material package/authority choices. |
| RP-19–20 | Closure delegation and release destination semantics are fixed before affected disposition/publication; unresolved authority or provider ambiguity blocks closeout rather than disappearing. |

The table is a deadline for resolution, not permission to defer a value already needed by an
earlier quality gate. Record each selection's version, scope, evidence, decision authority and
recheck trigger in its governing package/profile.

| ID | Decision or parameter | What is fixed | What still needs selection or evidence |
|---|---|---|---|
| RP-01 | Initial supported host/deployment matrix | Containerized Factory with dedicated Worker Docker; no host-installed Factory requirement. | Supported OS/architecture/container-runtime versions and resource floor. |
| RP-02 | HerdR deployment topology | Named owner/Worker sessions, explicit Pilot entry, read-only observation, no focus stealing, Factory-controlled resume. | Qualified version, identity/socket/process permissions, viewer implementation, headless operation, and resource ownership. |
| RP-03 | Initial harness/model matrix | Multi-harness and local/hosted models are supported architectural dimensions from day one. | Exact admitted harnesses/models/accounts and which required controls each demonstrates. |
| RP-04 | SQLite/Litestream integration | Transaction-bound remote durability, published frontier, exact supported restore lifetime. | Driver/PRAGMAs, replica version/configuration, synchronization mapping, retention mechanism, and proof under load/faults. |
| RP-05 | GitHub merge profile | PR merge only; no direct target push; head and base semantics represented honestly. | Merge method, required checks, branch freshness/protection rules, and any optional queue profile. |
| RP-06 | Triage batching | Semantic coherence, urgent release, bounded hold, closeout sweep. | Target/minimum/maximum batch sizing, aging/reconsideration cadence, priority contribution scales. |
| RP-07 | Validation scheduling | Target-based pending accounting and normal scheduler; normal target scheduling. | Pending threshold, age trigger, environment capacity, grouping limits, operator-request urgency. |
| RP-08 | Evidence independence | Implementer evidence may be accepted; Reviewer can gather evidence in the same review. | Project/risk profiles requiring selected independent checks and how they are justified. |
| RP-09 | Execution envelopes | All descendant/correction/review costs are cumulative at the owning scope. | Attempt/time/usage/storage bounds, reserves, severe-failure escalation thresholds. |
| RP-10 | Checkpoint frequency | Exact remote code checkpoints bound avoidable host-loss waste. | Maximum uncheckpointed interval/work and progress-checkpoint policy for each job type. |
| RP-11 | Recovery Kit | Private Git `bootstrap.json` plus `secrets.json.age`; independently retained repository access and age key; explicit initialization versus recovery. | Qualified age/release versions, protected key-input/storage mechanism, custody/rotation UX, and tested clean-host acquisition path. |
| RP-12 | Retention/cost | Historical metrics retained; required evidence/roots pinned; optional diagnostics bounded. | Artifact-class retention periods, storage/request budget alerts, protected checkpoint cadence. |
| RP-13 | Product performance | Measured query/publication/scheduler/recovery behavior under a declared workload. | Dataset size, concurrent project/attempt counts, latency/throughput targets, recovery-time target. |
| RP-14 | Standards source access | Pinned official sources; no invented clauses or false certification. | Licensed full text where necessary, source-locator completeness, reviewed normalization and mapping coverage. |
| RP-15 | Conditional standards | WCAG/ASVS/SLSA activate only for relevant scope and chosen targets. | Exact level/track/requirement selection per managed Project; no universal assumed level. |
| RP-16 | v1 code intelligence | Exact Git facts plus Serena semantic navigation/editing through a replaceable capability boundary. | Admitted Serena/backend versions, language coverage, licensing/telemetry settings, measured benefit, runtime setup. |
| RP-17 | Owner interface details | Pilot disposable; separate consequential confirmation; durable attention. | CLI command spelling, trusted confirmation transport, default briefing formatting, Bridge timing. |
| RP-18 | Artifact/record representations | Versioned JSON semantics and immutable/mutable separation. | Executable schema layout, ID encoding, Go types/packages, SQL normalization, pagination encoding. |
| RP-19 | Finding closure governance | No held or planned-only obligations hidden at closeout. | Exact owner delegation for no-action/risk/scope-change decisions and application to imported legacy findings. |
| RP-20 | Release providers | Named version sets and admitted publication profiles with partial-outcome handling. | Initial artifact registry/distribution destinations and their native idempotency/retention behavior. |
| RP-21 | Phase releases and review packages | Three exact-package owner releases; independent engineering gates; foundation versus feature packages; annotation provenance. | Project-specific budgets, package rendering/annotation controls, and material-change policies within those boundaries. |
| RP-22 | Provider projection profile | GitHub milestone maps to Initiative; optional planning representations; structured review history; separate mandatory PR controls. | Enabled views/fields/labels per Project, issue summary versus mirror settings, native review identities, drift/backfill and inspection-readiness policies. |
| RP-23 | Initial prompt and attack profiles | [Appendix F](worker-prompts.md#initial-role-and-job-prompt-library) defines role/job seed wording and [Section 15](../explanation/review.md#independent-review-and-the-current-correction-path) defines attack techniques; every admitted dispatch pins its contract. | Executable schema IDs, reviewed prompt revisions, role/job fixtures, and risk-specific mandatory probe selections before admission. |

## Proposed initial execution-handover profile

The following concrete profile received independent review and exact owner design approval ([#38 owner approval](https://github.com/blac9216/PriFly/issues/38#issuecomment-5701739109)). It does not admit any runtime tuple or release implementation. The preceding RP definitions remain the governing selection/qualification boundaries; this table selects the bounded initial scope and numerical targets before implementation observations. Runtime values still requiring actual identity/compatibility evidence are stated explicitly. Subsequent profile changes preserve prior evidence and require impact/requalification.

| ID / RP mapping | Proposed selection | Evidence and enforcement / recheck trigger |
|---|---|---|
| P1 / RP-01 | Linux x86_64, cgroup v2; separate Compose stack on existing host; reserve 8 vCPU, 16 GiB RAM and 80 GiB local disk for the entire PriFly profile, leaving host/development capacity outside that reservation. | Visible 16 CPU/~32 GiB in development is inventory, not reservation. Qualify actual host/kernel, engine/API, filesystem/UID/mount tuple and resource enforcement before admission. No host procurement. If unavailable, revise capacity/profile explicitly. |
| P2 / RP-02 | HerdR 0.9.0 protocol 22 candidate; private controller socket; ordinary panes running trusted launcher; resume/handoff/restart disabled; 2 attempt slots. | Pin exact binaries/images/schema/config. [V04](https://github.com/blac9216/PriFly/issues/38#issuecomment-5701521439) proves isolation, launch ambiguity, no focus stealing and cancellation. Recheck on any runtime/permission/mount change. |
| P3 / RP-03 | Codex CLI 0.154.0 and Claude Code 2.1.268 candidate interactive modes; owner-authorized subscriptions only; no metered API credentials or paid fallback. | Exact model+effort, account reference, plan entitlement, overage setting, refresh behavior and capacity observations must be in admitted Route manifests. Auth may be provided only through supported secret transport. Failures block the Route, not a switch of billing mode. |
| P4 / RP-04 | `modernc.org/sqlite` candidate v1.59.0 (observed package documentation), WAL/FULL/FK-on; Litestream 0.5.17 daemon; fresh lineage; retain-all remote; remote restore verification per publication; one CAS object, minimum 1.1 seconds between writes and bounded backoff. | Pin transitive SQLite/Go module versions and hashes after compatibility build. Exact source commit for Litestream is in #40. [V03](https://github.com/blac9216/PriFly/issues/38#issuecomment-5701521439)/[V10](https://github.com/blac9216/PriFly/issues/38#issuecomment-5701521439)/[V12](https://github.com/blac9216/PriFly/issues/38#issuecomment-5701521439) are admission evidence; no latest/timestamp restore. Changes to driver/replica/PRAGMAs/SDK require requalification. |
| P5 / RP-05 | GitHub App installation, PriFly repo only, no effective rule bypass; squash PR merge; strict trusted code+docs checks; actual effective native-approval rules. | Selected App/grants/rule/check producer identities and current target policy are recorded at admission. Contents/PR write and required read grants only; workflow-file update permission is added only for released work needing it. Verify base/head races against real GitHub. Policy drift blocks merge pending requalification. |
| P6 / RP-06 | External common-triage interface initially; coherent batch min/target/max = 1/1/3; urgent blocking Findings immediately visible; daily owner reconsideration reminder, maximum unreviewed hold 48 h, then durable Attention. | No automatic disappearance/closure at expiry. Factory stores every member/provenance/target/dependency. External planning can return reviewed batch Work Items; only actual disposition clears obligations. |
| P7 / RP-07 | 1 pending integration or oldest age 5 min triggers target eligibility; at most 2 compatible changes grouped; 1 validation environment; validation/correction/review preferred over new implementation when capacity frees. | Scheduling is ordinary bounded admission. [V08](https://github.com/blac9216/PriFly/issues/38#issuecomment-5701521439) tests stale result, backlog fairness, environmental block and new-revision accounting. No claim of calendar completion while subscription unavailable. |
| P8 / RP-08 | Reuse credible lower-risk producer evidence; independently rerun all changed trust/durability/merge-boundary oracles and modified acceptance tests. Reviewer may gather checks in its current review. | Selected required probes are fixed in each Work Item before execution. Reviewer independence concerns session/context, artifact and evaluator authority, not an assumption that two accounts are required for every code review. |
| P9 / RP-09 | Per normal code Work Item: total 8 Worker attempts across implementation, correction, review and rebase; max 2 serialization repairs per attempt; 90 min per attempt, 6 active Worker-hours total. Per target revision: max 3 Validator attempts, 30 min each, 90 min total. | Parent Initiative cap for the two-item handover: 20 Worker-hours, 24 attempts including target work, plus 2 Pilot model-hours and 40 Pilot requests. This is a **handover trial** envelope, not the estimate or authorization for implementing the whole skeleton. Defaults for delivery issues must be derived from their final scope. All descendants/rescues count; no counters reset on retry. |
| P9a / RP-09 | Each attempt ≤2 vCPU/4 GiB RAM/256 PIDs/8 GiB writable workspace growth; shared build/test Docker pool ≤2 vCPU/4 GiB; control services reserve ≥2 vCPU/4 GiB; max 2 attempts. Pilot shares an explicitly limited control-client budget, never the durable service reserve. | Total configured maxima fit P1 (4 CPU/8 GiB attempts +2/4 Docker +2/4 control). Disk admission uses measured usage + remaining admitted reservations. Rootless/cgroup capability is tested, not inferred. Pressure kills/pauses only according to lifecycle rules and keeps control paths available. |
| P9b / RP-09 | Lifecycle grace 10 s, forced-stop verification deadline 30 s; publisher health stale >60 s pauses new launches and requests safe checkpoints; critical safety faults immediately hold affected scope; 2 repeated same-category attempt failures raise Attention before further retry. | Killing for local safety during outage cannot acknowledge a canonical cancellation or grant a lease. No numeric token cap is invented where telemetry is unavailable: time/attempt/request/resource caps are enforced, token usage explicitly UNKNOWN. |
| P10 / RP-10 | Implementer/Rebaser progress checkpoint every 10 min of active writing and before handoff; 2 min cooperative/freeze completion allowance; missed checkpoint pauses attempt progression. Non-writing review/validation checkpoint durable findings/results at phase boundary and at least every 10 min where partial typed evidence exists. | Actual loss bound is ≤12 min active uncheckpointed writing under a functioning remote path; unavailable checkpoint blocks further writing after the deadline. Stop/freeze semantics must be proven before claiming this bound. No periodic auto-commit can race a writer. |
| P11 / RP-11 | age-encrypted secrets in private bootstrap Git; repository access and decryption identity independently held; startup reads an owner-selected exact manifest revision. | Pin age implementation/format, repository-fetch tool, OCI/bootstrap image digests and protected input mechanism. [V10](https://github.com/blac9216/PriFly/issues/38#issuecomment-5701521439) covers clean-host, wrong key/revision, unavailable remote, interrupted takeover. No key values are planning inputs. |
| P12 / RP-12 | Required remote data retain-all; initial accounted remote capacity envelope 40 GiB, warning at 24 GiB, stop new expensive admission at 32 GiB. No expiry for required prefixes/refs/keys. Optional terminal diagnostics ≤10 MiB/attempt, keep 7 days; per-attempt required artifact output reservation ≤128 MiB unless baseline increases it. | Before launch use P12b pre-send grants and reserve all outstanding grant maxima plus 8 GiB control/recovery margin; if accounting or retirement is unknown, hold admission. An attempt is not guaranteed to finish inside a grant; exhaustion pauses safely and never erases its obligations. Remote inventory/accounting includes replica compactions and archives, not just application evidence. At pressure, preserve pins and stop work; this is not a provider-enforced spend cap. No new paid provisioning is authorized by this proposal. |
| P12a / RP-12 | Live qualification envelope: ≤8 GiB new remote objects, ≤100,000 counted R2 API requests, isolated named prefix; 2 h aggregate subscribed harness use for early mechanism probes; no model API spend. | Record counts/bytes and stop before the envelope is exhausted; existing-provider storage/request charges can occur only under separately available owner-authorized service access. Retaining test roots is deliberate until their authorized retirement; no broad bucket cleanup. |
| P12b / RP-12 | A normal generation/adapter grant lasts at most 10 minutes and permits ≤1 GiB total upload bytes, ≤4,096 object writes and ≤10,000 total R2 requests. A separate control/recovery grant permits ≤768 MiB upload, ≤3,072 writes and ≤24,576 requests; single object ≤128 MiB, at most 2 forwarded writes, no unbounded multipart. Bootstrap grants use those same maxima. A publication ticket reserves ≤256 MiB upload, ≤1,024 writes and ≤8,192 requests; hold one complete ticket for the next publication and a second exclusively for renewal before committing ordinary work. New reservations cease at deadline; already precharged finite tickets survive expiry until resolved or charged fully unknown. | Budget gate charges full body before send, including compaction/snapshots/retries; grants are never silently reused/refilled. Reserve `verified existing required bytes + full unresolved/outstanding grant byte maxima + 8 GiB ≤ 40 GiB` before ordinary admission/renewal. At observed/accounted 32 GiB hold expensive work. Old-generation unknown grant is charged in full. Independent inventory and proved retirement may reconcile over-counting; no early credit. Request envelope P12a also bounds a qualification run across all renewed grants. See [C3](https://github.com/blac9216/PriFly/issues/38#issuecomment-5701520946). These byte limits cover R2; Git checkpoints have the separate measured 128 MiB/snapshot and 1 GiB/trial ceilings in [C4](https://github.com/blac9216/PriFly/issues/38#issuecomment-5701520946). |
| P13 / RP-13 | Qualification load: 1 Project/repo, 500 Work Items (including history), 10,000 Ledger events, canonical DB ≤64 MiB, 5–5.25 GiB required external data, 2 active attempts, 2 read clients; 1 authoritative command each 15 s for 30 min, plus burst of 10. | Healthy-path p95 published-query latency ≤500 ms, p99 ≤1 s; p95 publication ≤10 s, all ≤30 s (excluding declared injected faults); burst drains ≤120 s without lost/early ack. Scheduler decides within 5 s of published eligibility; publication time is measured separately. Exact fresh-host restore/verify/ACTIVE ≤30 min after prerequisites/network are ready. Use the fixed mix and method below, repeat three runs on recorded target tuple, and report every observation. Each run must pass separately; no favorable pooling. |
| P14 / RP-14 | Use baseline normalized `/v1` rubric criterion inventory and pinned sources; criterion descriptors must carry source/locator, method, applicability, blocking effect, evidence and independence before execution. | Missing precise source interpretation stays UNKNOWN. Catalog access is not full-text access. No full ISO/IEEE certification claim. Exact descriptor/source mapping is independently reviewed before making any imported criterion blocking. |
| P15 / RP-15 | No user-facing web UI/public API in this profile: CLI/local capability API only. WCAG web conformance and ASVS web-application target are not claimed; NIST SSDF-derived security profile applies. OCI build/release metadata includes source/tool/dependency/image digests and reviewed vulnerability/license dispositions; no formal SLSA level claimed. | These are explicit proposed applicability/assurance choices. Any public/browser endpoint or adopted SLSA assurance target triggers a new exact selection before admission. Secrets/transport/access/provenance tests still apply regardless of overlay label. |
| P16 / RP-16 | Serena local stdio with Go `gopls`; no IDE/plugin dependency; backend/tool settings pinned; workspace-scoped caches; no incidental GUI. | Source-backed support exists; exact commit/backend tuple and telemetry/license/config review precede tool admission. [V05](https://github.com/blac9216/PriFly/issues/38#issuecomment-5701521439) includes definitions/references/edit, stale index and unsupported coverage fixtures. No measured benefit claim before evidence. |
| P17 / RP-17 | CLI families: `bundle inspect/import`, `owner confirm`, `work list/show`, `attention list/show`, `attempt observe/cancel`, `validation request/show`, `command show`, `recovery inspect/takeover`, `upgrade inspect/activate`. Human and JSON output; paginated bounded queries. Thin Pilot binds only routine subset. | Names proposed, semantics in [C1](https://github.com/blac9216/PriFly/issues/38#issuecomment-5701520946)/[C2](https://github.com/blac9216/PriFly/issues/38#issuecomment-5701520946)/[C9](https://github.com/blac9216/PriFly/issues/38#issuecomment-5701520946). A clean-session operator must complete [H5](https://github.com/blac9216/PriFly/issues/38#issuecomment-5701521439); no direct SQL, raw Docker commands or bespoke repair scripts in the supported path. |
| P18 / RP-18 | Versioned JSON artifact bytes + relational identity/state indexes; random typed IDs, monotonic revisions, SHA-256, source/rendered identity; ordered 20-digit UTC migrations. | Schemas/round-trip/unknown-version fixtures and historical read compatibility precede admission. Exact schema IDs allocated in implementation under these contracts. |
| P19 / RP-19 | No standing delegation for risk acceptance, no-action or scope removal. Explicit owner confirmation per exact disposition, impact and retained obligations. | Pilot/Workers cannot hide Finding closure. Imported legacy Findings either have explicit provenance/obligation mapping or block relevant closeout. |
| P20 / RP-20 | Single self-installation release manifest plus immutable digest-addressed OCI archive in R2; no general release fan-out. | Owner-selected trusted bootstrap revision supplies expected image/archive digest. Upload-before-reference and possible-send reconciliation apply; supported recovery roots retain prior required releases/keys. |
| P21 / RP-21 | Feature package over PRD v2.1; three exact releases and independent gates. External review identity trust is separately owner-bound; deterministic rendered Markdown package + source hashes + explicit annotation locations. | No conversational “yes” interpreted against a newer unseen package. Planning preparation already authorized; these are product import/phase contracts, not invented retrospective Factory events for this session. |
| P22 / RP-22 | Reuse external planning issue/milestone mappings; mandatory PR/check/structured review publication; optional issue status comment only; full labels/board/roadmap projection disabled initially. | Disabled views are explicit, not silently stale enabled promises. Mandatory provider facts have exact identities and readiness checks. |
| P23 / RP-23 | Shared/role/job prompt v2 modules for 4 Worker roles plus separate Pilot contract; implementation, correction, implementation review, semantic rebase and validation variants admitted individually. | Schema/tool/authority/cold-start fixtures must pass before dispatch. Mandatory adversarial probes include tautological tests, false-success output, fabricated API/dependency, capability/result spoofing, hidden follow-up and unauthorized side effect, selected to job risk. |


### P13 fixed fixture and measurement method

The fixture generator is versioned and content-hashed with seed 9216. It creates 500 Work Items: 300 integrated/validated, 100 blocked on declared prerequisite conditions, 50 awaiting release and 50 eligible; 1,000 Findings (200 unresolved), 100 exact target revisions and 10,000 semantically valid Ledger events. Add content-addressed historical payload padding to make the canonical database 48–52 MiB initially, remaining ≤64 MiB throughout the run without deleting indexes/constraints; its 5–5.25 GiB remote dependency set consists of 80 verified 64 MiB evidence objects plus named Git checkpoints/keys. Reject a generator that cannot meet those identities/invariants rather than silently shrink the workload.

Each of 2 clients issues 2 queries/second on a predetermined repeating mix: 25% paginated Work Item list (50 rows), 25% Work Item detail with current blockers/dependency/evidence links, 25% Attention list (50 rows), 25% Validation Target history/detail (up to 50 results). Query keys rotate over the full dataset including empty/not-found cases (10% of each category); no benchmark-only shortcut query. Two attempts produce diagnostic output at 64 KiB/s each into the separately bounded optional telemetry path.

Every 15 seconds a scheduled authoritative command cycles through 25% valid package/annotation revision update carrying 256 KiB content-addressed artifact, 25% attempt/result transition with 1 MiB external evidence, 25% Finding/review result update with 64 KiB payload, 25% target or command-reconciliation update with 16 KiB payload. Prepare valid successive states so none is a no-op/replay; each changes Ledger/current state. At minute 15, add a burst of 10 valid commands of that same mix. Use three isolated fixture copies and equivalent isolated R2 prefixes; fixed data can be reused read-only but publication state/lineage cannot. No injected network faults are part of the healthy measurement run; fault tests remain separate and labeled.

Run an explicit cold-start measurement (empty derived caches/verification scratch) before a fixed 5-minute warm-up; report cold values separately with a ≤30 s first published-query readiness limit after ACTIVE. Then record 30 minutes of all request/response latencies, scheduled arrival time, queue delay, command acceptance/publication/ack clocks and generation/sequence. Percentiles use nearest-rank over all completed operations of each declared class in each run; failures/timeouts are failures, not dropped samples. Query p95/p99 must pass per query class and aggregate; publication p95/all and burst end-to-end completion include queueing. Publication deadline is measured from submission, not after a lane lock. Measured scheduling delay starts at published eligibility and excludes no undisclosed wait. Recover in a clean declared host using the exact 5 GiB closure and record every stage; the 30-minute bound starts once independently supplied prerequisites and working network are present. Capture actual link performance/config and report environmental failures rather than relaxing targets.
